<div class="hidden">
    {{#if prev}}
        <a id="prev" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}
    {{#if next}}
        <a id="next" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
</div>
